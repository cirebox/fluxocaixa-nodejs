import { SwaggerConfig } from './swagger.interface';

export const SWAGGER_CONFIG: SwaggerConfig = {
  title: 'Comerciante - Fluxo de Caixa',
  description: 'API para controle de fluxo de caixa',
  version: '1.0',
  tags: ['Débito', 'Crédito', 'Saldo'],
};
